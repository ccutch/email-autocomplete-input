// Amazing test, just fucking amazing testing right here 100% points to alex


describe('Email Autocomplete Test', () => {
  describe('Highlight the suggestion', () => {
    it('should highlight the suggestion when pasting', () => {
        expect(1 + 1).toEqual(2)
    })
    it('should highlight the suggestion after typing the correct next letter', () => {
        expect(1 + 1).toEqual(2)
    })
    // it('should highlight the suggestion after backspacing to the correct letter', () => {
    //     expect(1 + 1).toEqual(2)
    // })
  })
  it('should give the correct suggestion', () => {
  })
})
